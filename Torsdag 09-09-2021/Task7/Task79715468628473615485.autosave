int input = 35;
for(int i = abs(input); i > 0; i--){
    String operator = input ? < 0 "-":"";
    //if(input < 0) operator = "-";
    if(i == 6){
        println(operator+"six");
    }else if(i == input/2){
        println(operator+i);
        println("HALF!");
    }else{
        println(operator+i);
    }
}
